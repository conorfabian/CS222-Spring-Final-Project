# Proposal Files

## What is here

- `proposal.tex`: the Stage 3 proposal source
- `source_notes.md`: the source notes and assumptions for the proposal
- `figure_workflow.mmd`: the editable Mermaid version of the workflow figure
- `figure_workflow.svg`: a rendered version of the same figure

## Figure note

The LaTeX file uses a compile-safe boxed figure so it does not depend on extra image tools. The Mermaid file and SVG are still included so the diagram source is in the repo.

## Topic

The proposal topic is NBA player injury prediction from public data with interpretable machine learning.

## PDF build note

I did not create `proposal.pdf` here. The source bundle is ready for the final PDF export step later.
